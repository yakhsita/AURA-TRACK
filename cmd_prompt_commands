# to go to the sys user dir
cd %USERPROFILE%

# dir where we saved our file
cd yolodemo

# opening our file thru python
python demo.py

#installation process
python -m venv venv           #create virtual env in Windows
venv\Scripts\activate         #activate virtual env in Windows
python -m pip install --upgrade pip
pip install insightface
pip install onnxruntime
pip install onnxruntime==1.16.3
pip install opencv-python
pip install numpy
pip install ultralytics
